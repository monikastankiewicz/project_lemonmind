{% extends 'base.html.twig' %}
{% block title %}Nowy ładunek{% endblock %}
{% block body %}
    <div class="mt-10 max-w-2xl mx-auto">
        <h1>Zamów ładunek</h1>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn">{{ button_label|default('Save') }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
